#version 460 core

out vec4 FragColor;

in vec2 texCoords;

uniform sampler2D Texture;
uniform sampler2D Texture1;

float a = 0.2;

void main()
{
    FragColor = texture(Texture, texCoords) * (1.0 - a) + texture(Texture1, texCoords) * a;
}